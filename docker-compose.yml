services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: fixearn
      POSTGRES_USER: fixearn
      POSTGRES_PASSWORD: fixearn
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
